## Goal
Fix foxglove extension CI failure where x11 precondition fails due to missing `xauth`, ensuring foxglove tests pass in CI while keeping local success.

## Plan
See plan.md for implementation steps.
