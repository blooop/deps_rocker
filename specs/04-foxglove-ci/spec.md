## Goal
Fix foxglove extension CI failure where x11 precondition fails (missing `xauth`/`DISPLAY`), allowing headless CI runs while still enabling x11 when available.

## Plan
See plan.md for implementation steps.
