## Goal
Make lazygit extension builds deterministic in CI by avoiding GitHub “latest” lookups that yield empty versions; always install a pinned version with robust download handling.

## Plan
See plan.md for implementation steps.
