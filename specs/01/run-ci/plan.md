# Plan

1. Inspect existing CI setup and tooling requirements from `pyproject.toml` and Pixi tasks.
2. Run `pixi run ci` locally from the repo root.
3. Capture any failures, adjust configuration or dependencies if needed.
4. Re-run `pixi run ci` until the task completes without errors.
