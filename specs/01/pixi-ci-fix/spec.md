## Goal
- Run `pixi run ci` and address any failures so the pipeline passes locally.

## Non-Goals
- Introducing new features beyond fixes required for the CI run.

## Deliverables
- Passing `pixi run ci` execution with necessary code and test updates committed.
